You are a senior technical SEO + web platform auditor.

CONTEXT (DO NOT IGNORE):
- Live production site on Vercel
- Domains correctly configured in Vercel:
  - www.mapleleafpancakehouse.ca (PRIMARY)
  - mapleleafpancakehouse.ca → 301 redirect to www
- DNS confirmed valid
- SSL active
- Lighthouse score = 100
- SSR enabled (HTML rendered server-side)
- Goal: sanity-check LIVE OUTPUT, not theory

IMPORTANT:
- Assume Vercel handles redirects and domain canonicalization at the edge
- Do NOT recommend duplicate redirect logic
- Do NOT over-engineer
- Focus on what Googlebot actually sees

TASK:
Audit the LIVE SITE CODE AND RESPONSE for canonical correctness and SEO safety.

────────────────────────────────────
1. HTML CANONICAL TAG (CRITICAL)
────────────────────────────────────
Inspect the rendered <head> HTML on:
- https://www.mapleleafpancakehouse.ca/
- https://mapleleafpancakehouse.ca/

Verify:
- Exactly ONE <link rel="canonical">
- Canonical points to the PRIMARY domain (www)
- Canonical URL matches:
  - https protocol
  - trailing slash behavior
- Canonical is NOT:
  - relative
  - pointing to a redirected URL
  - dynamically inconsistent

Output:
- Canonical verdict: PASS / FAIL
- Exact canonical tag found

────────────────────────────────────
2. HTTP STATUS & REDIRECT BEHAVIOR
────────────────────────────────────
Check response behavior for:
- http://www.mapleleafpancakehouse.ca
- http://mapleleafpancakehouse.ca
- https://mapleleafpancakehouse.ca

Verify:
- All non-canonical variants return 301
- Final destination is canonical URL
- No redirect chains (>1 hop)

Output:
- Redirect flow table

────────────────────────────────────
3. META & INDEXATION SIGNALS
────────────────────────────────────
Inspect:
- <meta name="robots">
- Any X-Robots-Tag headers
- robots.txt

Confirm:
- index, follow is present
- No conflicting directives
- No accidental noindex

Output:
- Indexation safety verdict

────────────────────────────────────
4. OPEN GRAPH & SOCIAL CANONICAL ALIGNMENT
────────────────────────────────────
Verify:
- og:url exists
- og:url matches canonical EXACTLY
- No www / non-www mismatch
- No trailing slash mismatch

Output:
- Alignment status

────────────────────────────────────
5. STRUCTURED DATA URL CONSISTENCY
────────────────────────────────────
Inspect JSON-LD schema:
- Restaurant / LocalBusiness schema

Verify:
- "url" field matches canonical exactly
- No alternate domain references
- Menu URL correct (if present)

Output:
- Schema URL consistency verdict

────────────────────────────────────
6. DUPLICATE HTML RISK CHECK
────────────────────────────────────
Confirm:
- Same HTML is NOT served under multiple indexable URLs
- No soft-duplicate paths (e.g. /about, /contact) indexable
- SPA rewrites do not expose duplicate content to crawlers

Output:
- Duplicate risk: NONE / LOW / PRESENT

────────────────────────────────────
7. FINAL SANITY VERDICT
────────────────────────────────────
Provide a concise final judgment:

- Is canonical handling correct?
- Is Google likely to select the intended canonical?
- Is any HTML-level change REQUIRED?

Explicitly state:
- “No action needed” if configuration is already optimal
- Avoid speculative or cosmetic recommendations

FORMAT RULES:
- Be precise
- Quote actual tags/headers where relevant
- Prefer confirmation over change
- Assume the reader is technical

END GOAL:
Confirm the live site’s canonical + HTML SEO configuration is production-correct and future-safe.
